# AgentPort 2.0.4

- Aligns every sidebar icon to the same left guide.
- Builds the launcher as a Windows GUI application, preventing a terminal window from appearing first.
- Embeds the AgentPort icon into the EXE for Explorer, shortcuts and the taskbar.
- Keeps the same AgentPort icon on the running application window.

The exact public build passed PowerShell parsing, UI rendering and Windows icon verification.
