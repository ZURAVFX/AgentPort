# AgentPort 2.1.0

Unsloth Desktop and Hugging Face cache discovery now works on Windows PowerShell 5.1 as well as PowerShell 7. The scanner resolves both `Target` and `ResolvedTarget` symlink properties before applying its minimum-size filter.
