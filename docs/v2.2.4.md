# AgentPort 2.2.4

Fixed the Home stop controls failing on first use because the background stop-operation record did not initialise its handle property. Stop Harness, Stop backend and Stop all now enter their normal background operation path immediately.
