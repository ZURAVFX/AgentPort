# AgentPort 2.2.5

Multimodal GGUF models with a detected `mmproj` helper are now declared to DeepSeek Harness as accepting both text and image input. AgentPort already passes the helper to the local GGUF backend; this update also enables Harness image attachment and filesystem image inspection for Ridge and other compatible vision models.
