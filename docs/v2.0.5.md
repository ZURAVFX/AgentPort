# AgentPort 2.0.5

- Registers AgentPort's Windows AppUserModelID before creating the application window.
- Registers the AgentPort icon and relaunch command for the taskbar group.
- Prevents Windows from falling back to the PowerShell host icon while AgentPort is running.

The executable remains a Windows GUI application and keeps the embedded AgentPort icon resource.
