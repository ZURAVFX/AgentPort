# AgentPort 2.2.6

Adds the verified GitHub Release updater. Existing AgentPort launches check the latest stable release before starting, verify the downloaded executable against its SHA-256 asset, and restart into it only after the old process exits. Network failures or malformed releases leave the current installation untouched.
