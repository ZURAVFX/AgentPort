# AgentPort 2.1.1

Unsloth Desktop cache models are now labelled explicitly. On a fresh AgentPort profile, an installed GPT-OSS GGUF is selected first automatically. Existing user selections remain unchanged, and the tested Qwen3-Coder download remains the safe one-click recommendation until GPT-OSS receives equivalent MCP validation.
